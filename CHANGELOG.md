# 更新日志

## v1.1.0 - 2026-03-28

### 新增

- 新增离车安全提醒，检测未锁车、门窗未关、前后备箱未关、充电口未关。
- 新增胎压异常提醒，基于 TeslaMate `tpms_soft_warning_*` 实时推送异常轮位和胎压。
- 新增充电异常提醒，覆盖 `NoPower` 和未达到目标 SoC 即提前停止两类场景。
- 新增哨兵实时录制事件通知，基于 TeslaMate `center_display_state` 实时状态触发。
- 新增可选行程路况分析，支持在行程中低频采样高德交通态势，并把结果并入行程评分与分析文案。

### 优化

- 行程结束推送改为 100 分制驾驶评分，不再使用粗粒度 `A/B/C/D`。
- 驾驶评分改为按每 100 km 归一化急加速和急减速事件，并结合城市通勤 / 高速巡航 / 综合路况动态修正。
- 急加速判定从“纯功率突增”优化为“功率突增 + 速度明显提升”，减少误报。
- 行程结束通知样式重构，标题、副标题与内容分层更清晰，并补充分析摘要、建议和路况摘要。
- 月报移除驾驶评分展示，避免低价值噪声；日报和周报文案同步收敛。
- 补充环境变量文档和部署模板，覆盖全部实时提醒与路况分析开关。

### 兼容性与兜底

- 行程路况分析使用本地 JSON 缓存，不引入额外数据库。
- 高德交通态势接口无权限、超时、返回异常时，自动降级为无路况增强，不影响主推送。
- 本地缓存目录不可写时，自动降级为仅内存模式，不影响主推送。
- 行程结束时会等待路况汇总完成，避免主推送与路况增强抢跑。

### 许可证说明

- 本项目当前继续保留 `GPL-3.0`。
- TeslaMate 上游协议为 `AGPL-3.0`，但本项目默认通过 MQTT / PostgreSQL 读取数据，不直接分发 TeslaMate 源码。
- 若未来直接引入、修改或分发 TeslaMate 上游源码，需要单独重新评估 `AGPL-3.0` 合规要求。
